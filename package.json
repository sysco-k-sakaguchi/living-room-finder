{
  "name": "living-room-finder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Property sharing app for comparing co-living candidates on Vercel.",
  "scripts": {
    "check:app": "node --check app.js",
    "check:api": "node --check api/import-property.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
